'use client';

export default function OwnerReservationDetailLayout({ children }: { children: React.ReactNode }) {
  return <div className="flex flex-col gap-[0.625rem] bg-gray50 w-full h-full">{children}</div>;
}
